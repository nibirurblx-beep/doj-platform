# Discord OAuth setup (Phase 1I — placeholder)

Not needed yet. When Phase 1I begins this document will walk through:

1. Creating an application in the Discord Developer Portal (your account).
2. Adding OAuth2 redirect URLs (Supabase callback + production domain).
3. Recording the client ID and secret into environment variables.
4. Enabling the Discord provider in Supabase Auth with manual identity
   linking, and explicitly leaving automatic email-match linking disabled.

Do not create the Discord application yet unless you want to reserve the
name.
